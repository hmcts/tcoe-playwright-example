#!groovy
@Library("Infrastructure")

def type = "nodejs"
def product = "tcoe"
def component = "web"

def yarnBuilder = new uk.gov.hmcts.contino.YarnBuilder(this)

withPipeline(type, product, component, 600) {
  afterSuccess('build') {
    yarnBuilder.yarn('lint')
  }
}